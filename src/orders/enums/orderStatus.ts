export enum OrderStatus {
    DELIVERED = 'delivered',
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
    CANCELLED = 'cancelled',   
    PACKED = 'packed',
    SHIPPED = 'shipped', 
}